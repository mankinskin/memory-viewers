id = "397fa45b-a0bd-43d2-b430-2dfa44d80c5c"
created_at = "2026-05-25T11:48:36.383783700+00:00"
title = "[ticket-http][ticket-viewer] Expose workspace graph payload for focused full-graph navigation"
state = "in-review"
acceptance_criteria = "1. The frontend can request a workspace graph payload rather than only a fixed-depth root subgraph. 2. The payload supports focus changes without refetching a new root-local graph on every selection. 3. The payload preserves relationship metadata needed for efficient focus highlighting and culling."
component = "ticket-http"
implementation_summary = "Ticket-http now exposes a workspace-scoped graph payload and ticket-viewer fetches and caches that payload by workspace, so focus changes can reuse one graph dataset instead of refetching root-local subgraphs. Focused HTTP and release E2E coverage were updated for the new route and graph-mode selection flow."
priority = "high"
risk_level = "medium"
spec_refs = "8c4d51ef-c0b6-437d-bc14-672b0802cef2"
tags = "ticket-http,ticket-viewer,graph,transport,performance"
type = "tracker-improvement"
validation_summary = "Passed cargo test -p ticket-http workspace_graph_includes_isolated_local_and_cross_workspace_nodes -- --nocapture; cargo check --manifest-path memory-viewers/ticket-viewer/frontend/dioxus/Cargo.toml; cargo build --manifest-path memory-viewers/ticket-viewer/Cargo.toml --release; viewer-ctl install ticket-viewer; npm run test:e2e:release -- graph3d-fetch-error.spec.ts; npm run test:e2e:release -- graph-detail-sidebar.spec.ts."
