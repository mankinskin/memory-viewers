target/
.vscode/
agent-tmp/

# Local cargo configuration (personal overrides)
.cargo/config.local.toml

# Local tracing configuration (use config/tracing.toml.example as template)
config/tracing.toml
config/tracing.*.toml
config/test-config.toml
# Legacy locations (still supported)
.tracing.toml
tracing.toml
