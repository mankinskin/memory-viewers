id = "ee6e2d37-60b0-434f-8b8e-d2ccbb2f7624"
created_at = "2026-03-28T15:08:48.403901700+00:00"
title = "Bug: GPU canvas should cover the full page, not just hypergraph view"
state = "cancelled"
acceptance_criteria = "GPU canvas is position:fixed covering full viewport at z-index:-1 (opaque, behind all UI); smoke/particles visible on ALL tabs (not just hypergraph); global toggle to disable GPU shading; canvas resolution scales by devicePixelRatio for sharp HiDPI rendering; resize listener syncs canvas on window/viewport changes; HypergraphView registers 3D callback on shared overlay rather than owning the canvas"
component = "memory-viewers"
interview_answers = "Q1: GPU overlay always running on every tab, with global toggles to disable GPU shading. Q2: Proper HiDPI support (devicePixelRatio scaling), going beyond the original TS frontend. Q3: Opaque canvas at z-index:-1 (matching original approach)."
risk_level = "medium"
type = "tracker-improvement"
