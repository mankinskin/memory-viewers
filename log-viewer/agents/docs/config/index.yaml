name: config
description: Configuration loading from files and environment

files:
  - name: config.rs
    description: Configuration structure and loading from TOML files and env vars

key_types:
  - Config: Main configuration structure
  - ServerConfig: Server-specific settings
  - LoggingConfig: Logging configuration settings

submodules: []
source_files:
  - src/config.rs
last_synced: null
